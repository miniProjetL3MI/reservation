<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    tools:ignore="MissingLeanbackLauncher">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.touchscreen"
        android:required="false" />
    <uses-feature
        android:name="android.software.leanback"
        android:required="true" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Reservation"
        tools:targetApi="31">

        <activity
            android:name=".activities.SignUpActivity"
            android:exported="true" />
        <activity
            android:name=".activities.SignInActivity"
            android:exported="true" />
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ChoisirSpecialitesActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />
        <activity
            android:name=".SignupPatientActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />
        <activity
            android:name=".SignupAssistanteActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />
        <activity
            android:name=".ChoisirRendezVousActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />
        <activity
            android:name=".FormulaireMedcinActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />
        <activity
            android:name=".ChoisirTypeActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />
        <activity
            android:name=".ActivityChoisirMedecin"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />

        <activity
            android:name=".ProfilMedecinCotePatient"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />

        <activity
            android:name=".ProfilCoteMedecinActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />
        <activity
            android:name=".ProfilPatientActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />

        <activity
            android:name=".ModifierProfilPatientActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />
        <activity
            android:name=".EvaluerActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />
        <activity
            android:name=".NotificationPatientActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />

        <activity
            android:name=".NotificationMedecinActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Reservation" />

    </application>

</manifest>